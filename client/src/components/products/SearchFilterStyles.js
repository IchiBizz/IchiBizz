const useStyles = theme => ({
  card: {
    maxWidth: 100
  },
  root: {
    width: 300
  },
  filterContainer: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center"
    // alignContent: "center"
  }
});

export default useStyles;
